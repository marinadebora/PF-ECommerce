let {Router} = require("express")
let embarcacionesV = require("../modelos/EmbarcacionesVenta")

let getEmbarcacionesVenta = Router();

 getEmbarcacionesVenta.get("/", async (req,res)=>{
    let controlador = await getEmbarcacionesVenta 
})
